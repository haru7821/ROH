# Jarvis — 개인 음성 비서 (Windows, Python 3.11+)
# 상한을 두어 다음 메이저 버전에서 조용히 깨지는 것을 막는다.
anthropic>=1.4,<2

# 오디오 입출력
sounddevice>=0.4.6,<1
soundfile>=0.12.1,<1
numpy>=1.26,<3

# 음성 인식 (로컬, API 키 불필요)
faster-whisper>=1.0.3,<2

# 전역 단축키
pynput>=1.7.6,<2

# 음성 합성 (edge = 온라인 뉴럴 보이스, sapi = 윈도우 내장 폴백)
edge-tts>=6.1.9,<8
pyttsx3>=2.90,<3

# PC 제어
pyperclip>=1.8.2,<2

# 메일 / 일정 (선택 — 이 기능 안 쓰면 설치 안 해도 됨)
google-api-python-client>=2.120.0,<3
google-auth-httplib2>=0.2.0,<1
google-auth-oauthlib>=1.2.0,<2
