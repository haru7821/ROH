# ROH 개발 규칙

Rune of Hell — UE 5.8 / GAS 기반 싱글플레이 핵앤슬래시 ARPG.
설계 문서는 `docs/01`~`09`, 마일스톤 로드맵은 `docs/04`.

## 코드 변경 프로세스 (필수) — CTO/Pro/Sup 체제

- **CTO(메인 세션)**: 마일스톤 우선순위와 설계 결정, 최종 커밋·푸시, 사용자 보고
- **Agent Pro(구현 에이전트)**: CTO의 스펙을 받아 코드 작성
- **Agent Sup(검수 에이전트)**: 푸시 전 정적 분석으로 전수 검증
  - 컴파일 오류 (UE 5.8 API/IWYU include/UHT 규칙, 섀도잉=에러)
  - GAS 사용 오류, 런타임 크래시 위험 (타이머/델리게이트 수명, 불완전 타입)
  - 로직 버그
  - 단, 한 줄 수준의 자명한 수정(오타, 수치, 로그 문구)은 생략 가능

절차: CTO 결정 → Pro 구현 → Sup 검수 → CTO가 지적사항 반영 후 커밋·푸시 →
사용자는 `update.bat` 으로 수신 (Pull → 클린 빌드 → 에디터 실행).
클라우드 환경에는 UE가 없어 실행 검증은 사용자 로컬에서만 가능하다.

## 빌드 검증 규칙

- 이 저장소는 CI 없는 환경에서 작성될 수 있으므로, 게임플레이 코드 변경 시
  `Source/ROH/ROH.h` 의 **`ROH_BUILD_TAG` 를 반드시 증가**시킬 것 (b8 → b9 → ...).
  화면 좌상단 상태 텍스트로 사용자가 실행 중인 빌드를 즉시 식별한다.
- 로컬 재빌드는 반드시 `update.bat` 경유 (에디터 더블클릭은 스테일 DLL 함정 있음).

## 부속 도구: tools/jarvis (게임 코드 아님)

`tools/jarvis/` 는 소유자용 개인 음성 비서(Python, Claude API)다. 게임 런타임과
무관하므로 `ROH_BUILD_TAG` 증가 규칙과 UE 빌드 검증 대상이 아니다.
설정(`config.toml`)·토큰(`secrets/`)·런타임 데이터(`data/`)는 커밋 금지.
사용법은 `tools/jarvis/README.md`.

## 아키텍처 원칙 (docs/03 요약)

- 모든 스탯 보정은 GameplayEffect로만 (장비/스킬/정복자/버프)
- 스킬/아이템/드랍은 데이터 주도 (현재 C++ 레지스트리, DataTable 이관 가능 구조)
- 키 배치는 코드가 단일 소스 (`ROHPlayerController::BuildRuntimeInput`)
- 그레이박스 단계: 시각 요소는 애셋 의존 금지 (디버그 드로잉/폴백 필수)
- 멀티플레이 확장 대비: 시뮬레이션과 표현 분리 유지

## 마일스톤 현황

- M0~M2 완료 (로컬 검증됨)
- M3 1차 구현: 경험치/레벨업, 스킬트리(랭크+시너지, 클래스당 3스킬),
  원소술사(마법탄/화염구/서리신성/텔레포트), 세이브/로드(ROHSave/ROHLoad),
  클래스 전환(ROHSetClass)
- M3 2차 구현: 스킬트리 확장 — 클래스당 10스킬 × 3계열
  (전사: 무기술/전장의 함성/투지, 원소술사: 화염/냉기/번개),
  액티브/패시브 구분(패시브는 런타임 무한 GE), 신규 액티브 6종
  (처형/전투의 함성/얼음화살/운석/눈보라/정전기장), 스킬 슬롯 4개
  (키 1~4, ROHBindSkill로 배치), 리스펙(ROHRespec)
- M4 1차 구현: 슬롯 배치 세이브 연동, 보스 발타르(ROHSpawnBoss —
  내려찍기 장판 패턴/광폭화/TC_Boss 드랍/화면 HP 표시)
- M4 2차 구현: 난이도 3단계(ROHSetDifficulty — 몬스터 스케일링/저항 페널티/
  ilvl 상승, 스폰 시 적용), 액트 보스 모르가스(ROHSpawnBoss 2 — 원거리 카이팅/
  화염탄 3연발/하수인 소환), 메인 퀘스트 라인(들판 정리 10 → 발타르 → 모르가스,
  단계 보상, HUD 목표 표시, 캠페인 상태 세이브 연동, UROHCampaignSubsystem)
- M4 3차 구현: 몬스터 확충(덩치/주술사/추적자 — 총 6종+보스 2),
  정예 변형(챔피언 10%/유니크 3%, 발밑 링 표시, 추가 드랍/MF),
  전투 알고리즘 1단계(docs/10 채택 — 치명타/저항 6속성/물리 피해감소/
  피해 분산/룬 배율 RunePower, 난이도 저항 페널티 -25/-50, 신규 접사 5종)
- 전투 알고리즘 2단계(docs/10 §2/§3.4/§4.1 — b20): HP/MP 공식
  (클래스 기본치+레벨당 성장+VIT×5/INT×2), 체력/마나 재생(VIT·INT 기반+접사),
  공격/시전 속도(쿨다운 단축, DEX·INT 기반+접사 4종), 방어등급 VIT 배율
- 전투 알고리즘 3단계(docs/10 §1/§3.1 완전형 — b29): % 배율 어트리뷰트 7종
  (%HP/%MP/%재생 2종/%물리/%원소/%전체 피해 — MultiplyAdditive 무한 GE,
  %0이면 미적용), AP/SP 완전형(물리 ×(1+STR/100+물리%)×(1+전체%),
  원소 ×(1+INT/100+원소%)×(1+전체%) — 어빌리티 11곳 자체 스탯 항을 파이프라인
  이관, 몬스터 STR/INT=0으로 회귀 차단), % 접사 5종+상위 유니크/세트 % 옵션.
  성장/분배는 SetNumericAttributeBase 기준 가감 (Set(Get()+Δ) 금지 —
  장비/배율 GE가 베이스에 눌어붙는 버그 방지, ProgressionAddToBase)
- 스킬트리 증분 (b30): 클래스당 10→15스킬 (액티브 2+패시브 3 추가) —
  전사: 돌진(경로 스냅샷 판정)/광란(8초 공속 Duration GE)/학살 본능(물리%)/
  강골(%HP)/전장 회복(재생), 원소술사: 불꽃 파도(전방 콘)/연쇄 번개(최대
  3연쇄 ×0.7)/연소 정통(원소%)/얼음 결정(%마나)/과충전(전체%).
  % 패시브는 b29 배율 어트리뷰트 재사용 (투자 즉시 HP/MP 갱신)
- M4 4차 구현: 지역 4개(재의 마을/잿빛 들판/무너진 지하묘지/재의 성소 —
  UROHZoneManager가 존별 스포너·보스·웨이포인트 자동 배치, 존 레벨 보너스),
  웨이포인트(E 순환 이동/ROHWarp, 활성화 세이브 연동), 보스 선행 처치 자동 정산,
  내비 RuntimeGeneration=Dynamic. 에디터 맵 확장 가이드: docs/11
  M4 코드 완료. 에디터 맵 확장 가이드: docs/11
- M5 1차 구현: 룬 시스템 — 룬 12종(엔~아자크, 티어별 소켓 보너스+RunePower
  티어×0.5%) 드랍(TC 룬 항목, ilvl 티어 게이트)/3:1 합성(ROHTransmute),
  소켓(장비 생성 시 0~3, ROHSocket), 룬워드 8종(일반 등급+정확한 순서,
  ROHRunes로 목록) — 장착 GE에 DB 실시간 해석, 세이브 호환
- M5 2차 구현: 유니크 15종(성인 명명, 고정 옵션, 드랍 1%+MF) — 분해(ROHSalvage
  → 성유물 조각)/고대 합성(ROHForgeAncient — 조각 5개, 옵션 x1.5+룬위력+3, 진홍),
  세트 4종(고대 유적지 명명 — 잿빛 첨탑3/가라앉은 묘역2/아자크론의 잔해5/
  침묵의 제단3, 장착 수 임계 보너스, 드랍 1.5%+MF, ROHSets/ROHUniques)
  등급 색상 확정(흰/파랑/노랑/초록 세트/금 유니크/자주 룬워드/진홍 고대)
  남은 것: docs/10 미적용분(§1 % 배율, §3.1 AP/SP 완전형), 트리 증분, UI,
  M5 잔여(정복자/스태시)
- UI 1차 구현 (b24, C++ UMG — 애셋 없음): 웨이포인트 지역 선택 창(E, 사용자
  결정 반영 — 순환 이동 대체), 인벤토리/장비 창(I — 등급색/클릭 장착·해제·물약,
  세트 집계), 스킬트리 창(K — 계열별 [+] 투자, 슬롯 [1~4] 배치). UI/ 모듈,
  UROHUiWindow 공통 베이스. 남은 UI: 툴팁/드래그/미니맵/창 입력 차단(2차)
- UI 2차 구현 (b28): 창 열림 중 게임 입력 차단(이동/공격/스킬 — 토글 키는 유지,
  스테일 클릭 이동 방지 포함), 아이템 툴팁(호버 상세 — ResolveEquipBonuses
  공용 해석 헬퍼로 장착 GE와 수치 단일 소스, 미감정은 옵션 숨김,
  인벤토리/스태시/벤더 전 창 적용), 정복자 창(P — 레벨/XP 진행/4분류 [+1] 투자,
  만렙 미만 잠금). 드래그/미니맵/창 자동 갱신은 UI 3차·M6로 이월
- UI 3차 구현 (b31): 창 자동 갱신(컴포넌트별 ChangeSerial 단조 카운터 +
  UROHUiWindow dirty 폴링 — Serial 불변 시 리빌드 없음, RefreshNow로 캐시
  동기화), 스킬바 HUD(하단 상시 — 슬롯 5개 이름/쿨다운 잔여/리소스 부족 색,
  버프 잔여 표시, HitTestInvisible), 미니맵(우상단 200px — NativePaint 점
  그리기, 0.25s 액터 캐시 TWeakObjectPtr, 몬스터 빨강/정예 주황/보스 자주/
  NPC 초록/웨이포인트 파랑/보관함 노랑). 드래그는 M6 이후로 이월
- M6 1차 구현 (b32): 애셋 카탈로그 (Core/ROHAssetCatalog — /Game/ROH/Art/
  규약 경로에 임포트만 하면 코드 수정 없이 교체, DoesPackageExist 선확인으로
  미임포트 시 무로그·그레이박스 폴백 100% 유지, SM_→M_ 머티리얼 규약).
  훅업: 캐릭터 10종(GetCatalogMeshKey 가상)/웨이포인트/보관함/NPC 역할별/
  투사체 속성별. 소유자 가이드: docs/13 (Fab 무료 팩 추천/키 표/절차).
  M6 2차 예정: 스켈레탈+애니, 3차: 나이아가라
- M6 1.5차 (b33): 프로시저럴 비주얼 — 엔진 기본 도형 조합+BasicShapeMaterial
  MID 채색으로 임포트 없이 실루엣 조형 (Core/ROHProceduralVisual — AddPart/
  BuildCharacterVisual). 우선순위: 카탈로그 애셋 → 프로시저럴 → 그레이박스.
  웨이포인트(받침+기둥+회전 크리스탈, 활성 색 전환)/상자(뚜껑+금띠)/
  NPC(역할색 로브 인체)/플레이어 2종(대검/지팡이)/몬스터 6종+보스 2
  (종별 실루엣 — 기울임/뿔/모자/수평몸통 등)/투사체 속성색.
  발광은 미지원(BasicShapeMaterial 한계) — M6 3차 커스텀 머티리얼로
- 버그 수정 (b34): 마을 안전지대 강제 — 맵(L_Sandbox.umap)에 M1/M2 잔재
  스포너가 원점에 저장돼 마을에서 몬스터가 스폰되던 문제. 코드가 맵 상태와
  무관하게 보장: 지역 매니저 미소유 스포너는 무조건 제거(IsZoneOwned),
  마을 반경 침입 몬스터는 1초 폴링 정리(OnDeath 브로드캐스트로 스포너
  개체 수 회수 — 직접 Destroy는 재스폰 카운트 누수), 보스/치트 소환분은 면제.
  존 앵커는 실제 플레이어 폰 위치(PlayerStart 다중 맵 대비).
  ※ 맵 정리(잔재 스포너·PlayerStart 3개·빈 액터 삭제)는 에디터에서 별도 권장
- M5 3차 구현 (b25): 도박 — 운명의 보석(악몽 3%/지옥 6%, 보스 2배 드랍),
  ROHGamble(보석 3개: 레어 50/유니크 30/세트 15/고대 잭팟 5%),
  잭팟 시 고대무기(성인 유니크 무기의 [고대]) + 축하 연출(그레이박스 링,
  최종안: M6 나이아가라 교체). ROHGiveGem 치트
- M5 4차 구현 (b26): 마을 NPC 6인 (docs/12 — 로사 잡화/브란 대장간/일렌 보석/
  미로 물약/카론 도박장/셀바 감정, 소유자 지시: 시나리오별 마을+NPC 구성),
  존 매니저가 마을에 자동 배치, E 대화 → 벤더 창(구매/판매/도박/감정),
  미감정 시스템(마법+ 드랍 미감정 — 장착/소켓/분해 차단, 감정 50골드/ROHIdentify)
- M5 최종 구현 (b27): 정복자 — 만렙(50) 초과 XP를 계정 정복자 XP로 라우팅
  (곡선 500+L×250, 레벨당 1포인트), 4분류 투자(공격 AP+1/방어 HP+5/
  정밀 치명타+0.1%/룬 조율 RunePower+0.2, 분류당 100 상한, 무한 GE로 적용,
  ROHParagon/ROHParagonUp) + 계정 보관함 — 마을 스태시 상자(E, 60칸,
  UROHStashWindow 입출금), 별도 슬롯 "ROH_Account"(UROHAccountSubsystem,
  입출금/투자 즉시 저장 + 캐릭터 슬롯 동시 스냅샷으로 소실/복제 차단),
  정복자/스태시는 클래스 전환·캐릭터 로드와 무관하게 유지. M5 완료
